import { IScoll5Directive } from './i-scoll5.directive';

describe('IScoll5Directive', () => {
  it('should create an instance', () => {
    const directive = new IScoll5Directive();
    expect(directive).toBeTruthy();
  });
});
