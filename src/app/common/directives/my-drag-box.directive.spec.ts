import { MyDragBoxDirective } from './my-drag-box.directive';

describe('MyDragBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDragBoxDirective();
    expect(directive).toBeTruthy();
  });
});
