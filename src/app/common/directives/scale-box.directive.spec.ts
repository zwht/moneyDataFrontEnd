import {ScaleBoxDirective} from './scale-box.directive';

describe('ScaleBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleBoxDirective();
    expect(directive).toBeTruthy();
  });
});
